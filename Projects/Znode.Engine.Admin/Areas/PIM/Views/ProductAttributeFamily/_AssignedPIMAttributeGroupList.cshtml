@using Znode.Engine.Admin.Models
@model Znode.Engine.Admin.ViewModels.PIMAttributeGroupListViewModel
@{
    string actionName = ViewContext.Controller.ValueProvider.GetValue("action").RawValue.ToString();
    string controllerName = ViewContext.RouteData.Values["controller"].ToString();
}
<div id="grid-container dirtyignore">

    <div class="col-sm-12 list-container">
        <div class="pull-right margin-bottom">
            @{Html.RenderAction("GetUnAssignedAttributeGroups", controllerName, new { @id = @Model.PimAttributeFamilyId, @area = "PIM" });}
        </div>
        @if (Model?.AttributeGroupList?.Count > 0)
        {
            @Html.Partial("~/Areas/PIM/Views/ProductAttributeFamily/_AssignedPIMGroupCollapsibleGrid.cshtml", Model)
        }
        else
        {
            <div class="col-sm-12 no-record nopadding">
                <p>@Admin_Resources.NoRecordFoundText</p>
            </div>
        }
    </div>

   
   
</div>